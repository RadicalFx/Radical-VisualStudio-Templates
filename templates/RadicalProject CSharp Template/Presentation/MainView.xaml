<Window x:Class="RadicalTemplate.Presentation.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:markup="http://schemas.topics.it/wpf/radical/windows/markup"
        xmlns:behavior="http://schemas.topics.it/wpf/radical/windows/behaviors"
        xmlns:rg="http://schemas.topics.it/wpf/radical/windows/presentation/regions"
        Title="MainView" Height="300" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBox Height="23"
                 Margin="10,10,0,10"
                 TextWrapping="Wrap" Text="{markup:EditorBinding Path=SampleText}" 
                 HorizontalAlignment="Stretch"
                 VerticalAlignment="Top" />
        <TextBlock Margin="10,10,10,10" 
                   Grid.Row="1" 
                   TextWrapping="Wrap" 
                   Text="{Binding Path=SampleText}" 
                   HorizontalAlignment="Stretch" 
                   VerticalAlignment="Top"/>
    </Grid>
</Window>
